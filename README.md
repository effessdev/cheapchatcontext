# CheapChatContext

## Usage

Run the command `ccc` from the project root. `ccc` literally means "copy context to clipboard", and does exactly that.

## What is included

1. The project structure
2. Files that you specify (either command line or config file)

You can use glob patterns (`*` and `**` are supported).
